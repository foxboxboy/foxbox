JSON: Reads and writes JSON that keeps its Godot types. A file carries a version, so an older one still loads after the format moves.

For a save nobody opens, FileAccess.store_var is two lines and keeps every type exactly.
