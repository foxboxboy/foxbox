JSON: Reads and writes JSON that keeps its Godot types. A file carries its format, and an older one migrates forward on read.

For a save nobody opens, FileAccess.store_var is two lines and keeps every type exactly.
